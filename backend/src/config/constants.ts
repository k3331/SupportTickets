/**
 * Application constants (runtime values from shared types).
 */

import { PRIORITIES, STATUSES } from '../../shared/types.js';

export const VALID_PRIORITIES = [...PRIORITIES];
export const VALID_STATUSES = [...STATUSES];
